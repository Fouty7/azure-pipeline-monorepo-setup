apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgres-pgvector.fullname" . }}-test-connection"
  labels:
    {{- include "postgres-pgvector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgres-pgvector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
